#ifndef PANOD_H
#define PANOD_H
	#include "PANod.h"
#endif

struct PALista
{
	struct PANod head;
};

int size(paLista lista);
paNod get(int position);
paNod head(paLista lista);
paLista tail(paLista lista)
paNod init(paLista);
//toArrayList();

