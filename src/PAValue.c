#include "../include/PAValue.h"
#include <defs.h>
INT op_Equality(struct PAValue one,struct PAValue other) {
    return 1;
}
INT op_Inequality(struct PAValue one ,struct PAValue other) {
    return 1;
}
INT op_GreatherThan(struct PAValue one,struct PAValue other) {
    return 1;
}
INT op_LessThan(struct PAValue one,struct PAValue other) {
    return 1;
}
INT op_GreatherThanOrEqual(struct PAValue one,struct PAValue other) {
    return 1;
}