#include <PA/Value.h>
#include <defs.h>
PA_INT op_Equality(struct PAValue one,struct PAValue other) {
    return 1;
}
PA_INT op_Inequality(struct PAValue one ,struct PAValue other) {
    return 1;
}
PA_INT op_GreatherThan(struct PAValue one,struct PAValue other) {
    return 1;
}
PA_INT op_LessThan(struct PAValue one,struct PAValue other) {
    return 1;
}
PA_INT op_GreatherThanOrEqual(struct PAValue one,struct PAValue other) {
    return 1;
}
