#include <PA/Value.h>
#include <defs.h>
PAInt op_Equality(struct PAValue one,struct PAValue other) {
    return 1;
}
PAInt op_Inequality(struct PAValue one ,struct PAValue other) {
    return 1;
}
PAInt op_GreatherThan(struct PAValue one,struct PAValue other) {
    return 1;
}
PAInt op_LessThan(struct PAValue one,struct PAValue other) {
    return 1;
}
PAInt op_GreatherThanOrEqual(struct PAValue one,struct PAValue other) {
    return 1;
}
