//@Author Cristofor Rotsching
#include "../include/PAList.h"
#include <defs.h>
INT Size(struct PAList lista) {
    int size = 0;
//    PATip tip = lista.Nod.Tip;
    struct PAElement nod_head = PAListaHead(lista);
    struct PATip tip;
//    return tip;
    return size;
    // if (nod_head.paTip == LAST(lista.))
    return 1;
}
struct PAElement PAListaGet(INT position) {
    struct PAElement nod;
    return nod;    
}
struct PAElement PAListaHead(struct PAList lista) {
	struct PAElement nod;
	return nod;
//    return lista.Nod;
}
struct PAList PAListaTail(struct PAList lista) {
    return lista;
}
struct PAElement PAListaInit(struct PAList lista) {
	struct PAElement nod;
	return nod;
//    return lista.Nod;
}

void PAListaPrint(struct PAList lista) {
    
}
